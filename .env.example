# Supabase Configuration
# Get these from your Supabase project settings: https://supabase.com/dashboard/project/_/settings/api
# See: https://supabase.com/docs/guides/api/api-keys

# Server-side only (NEVER expose to client) - bypasses RLS
# New format: sb_secret_... (replaces deprecated service_role key)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SECRET_KEY=sb_secret_your-secret-key-here

# Client-side (safe to expose in browser) - respects RLS
# New format: sb_publishable_... (replaces deprecated anon key)
# The VITE_ prefix exposes these to the frontend build
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your-publishable-key-here

# Application URL (for OAuth callbacks)
APP_URL=https://your-replit-app-url.replit.dev

# ============================================
# SendGrid Email Configuration
# ============================================
# Get your API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.your-api-key-here

# From email address (must be verified in SendGrid)
SENDGRID_FROM_EMAIL=noreply@claimsiq.com
SENDGRID_FROM_NAME=Claims iQ

# ============================================
# Invite System Configuration
# ============================================
# Number of days before invites expire (default: 7)
INVITE_EXPIRATION_DAYS=7

# Length of invite token in bytes (default: 32, generates 64 hex chars)
INVITE_TOKEN_LENGTH=32

# ============================================
# Password Reset Configuration
# ============================================
# Number of hours before password reset tokens expire (default: 24)
PASSWORD_RESET_EXPIRATION_HOURS=24
